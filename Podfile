# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '组件化' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'LGDNetworking', :path => 'lib/LGDNetworking'
  pod 'LGDLogining', :path => 'lib/LGDLogining'
  # Pods for 组件化
  
  target '组件化Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '组件化UITests' do
    # Pods for testing
  end

end
